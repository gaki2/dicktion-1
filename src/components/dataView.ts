import DOM from "../utils/index.js";

export default class DataView {
  createWrapper() {
    this.$wrapper = DOM.createEl("div", "m-auto mt-5 div_width_50");
  }
  render($parent: HTMLElementw) {}
}
